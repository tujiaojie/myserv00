#!/bin/bash

# --- 基础配置 ---
USER_NAME="tujiaojie"
REPO_NAME="myserv00"
WORKDIR="$HOME/mynode"
SB_BIN="$WORKDIR/sing-box"
CONFIG="$WORKDIR/config.json"
RAW_URL="https://raw.githubusercontent.com/$USER_NAME/$REPO_NAME/main"

# 颜色定义
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

install_core() {
    if [ ! -f "$SB_BIN" ]; then
        echo -e "${YELLOW}正在下载核心程序...${NC}"
        mkdir -p $WORKDIR && cd $WORKDIR
        wget https://ghproxy.net/https://github.com/SagerNet/sing-box/releases/download/v1.8.5/sing-box-1.8.5-freebsd-amd64.tar.gz
        tar -zxvf sing-box-1.8.5-freebsd-amd64.tar.gz
        mv sing-box-1.8.5-freebsd-amd64/sing-box .
        chmod +x sing-box
        rm -rf sing-box-1.8.5-freebsd-amd64*
    fi
}

sync_config() {
    echo -e "${YELLOW}正在同步 GitHub 配置...${NC}"
    mkdir -p $WORKDIR
    wget -q -O $CONFIG "$RAW_URL/config.json"
    if [ $? -ne 0 ]; then
        echo -e "${RED}❌ 同步失败！${NC}"
    else
        echo -e "${GREEN}✅ 配置已成功同步！${NC}"
    fi
}

show_menu() {
    clear
    echo -e "${GREEN}======================================${NC}"
    echo -e "${YELLOW}    tujiaojie 专属 Serv00 节点管理    ${NC}"
    echo -e "${GREEN}======================================${NC}"
    echo -e "  1. 启动/重启 节点"
    echo -e "  2. 查看 节点分享链接"
    echo -e "  3. 停止 代理服务"
    echo -e "  4. 卸载 代理服务"
    echo -e "  0. 退出脚本"
    echo -e "${GREEN}======================================${NC}"
    read -p "请输入选项: " choice

    case $choice in
        1) 
            install_core
            sync_config
            pkill -9 sing-box
            cd $WORKDIR
            nohup ./sing-box run -c config.json > /dev/null 2>&1 & 
            echo -e "${GREEN}🚀 节点已在后台启动！${NC}"
            sleep 2; show_menu ;;
        2) 
            [ ! -f "$CONFIG" ] && echo "请先选择 1 启动节点" && sleep 2 && show_menu
            # --- 核心精准提取逻辑 ---
            PORT=$(grep '"listen_port":' $CONFIG | tr -cd '0-9')
            UUID=$(grep '"uuid":' $CONFIG | awk -F'"' '{print $4}')
            # 这里的改进：精准匹配 path 关键字后的第四个引号内容
            PATH_WS=$(grep '"path":' $CONFIG | awk -F'"' '{print $4}')
            IP=$(curl -s ipv4.icanhazip.com)
